import BigContent from 'src/layout/BigContent';

export default function TermsConditions() {
  const data_head = {
    title: 'Terms & Conditions',
    description: 'Texto de prueba',
  };

  return (
    <BigContent data_head={data_head}>
      <p>test</p>
    </BigContent>
  );
}
